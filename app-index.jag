<%
var caramel = require('caramel');
var uriMatcher = new URIMatcher(request.getRequestURI());
caramel.render({
    appname : uriMatcher.match('/{context}/webapps/{webapp}').webapp,
    error: [],
    config: require('/config/config.json')
});
%>
